
<h1>Trips</h1>
 
<table>
  <tr>
    <th>Start Time</th>
    <th>End Time</th>
    <th></th>
  </tr>
 
<% @trips.each do |trip| %>
  <tr>
    <td><%= trip.start_time %></td>
    <td><%= trip.end_time %></td>
    <td><%= link_to 'Show', trip_path(trip) %></td>
    <td><%= link_to 'Name Starting Location', new_location_path(:vehicle_id => params[:vehicle_id], :point => trip[:start_location]) %></td>
    <td><%= link_to 'Name Ending Location', new_location_path(:vehicle_id => params[:vehicle_id], :point => trip[:end_location]) %></td>
  </tr>
<% end %>
</table>
 
<br>