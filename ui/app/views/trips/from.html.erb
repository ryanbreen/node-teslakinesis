
<div class="page-header">
  <h1>
   Trips from <%= @from.name %>
   <small><i class="ace-icon fa fa-angle-double-right"></i> sorted by most common</small>
 </h1>
</div>

<div class="row"><div class="col-xs-12">
  <table id="simple-table" class="table table-striped table-bordered table-hover">
    <thead>
      <tr>
        <th>Destination</th>
        <th>Number of trips</th>
      </tr>
    </thead>

    <tbody>
<% @endpoints.each do |endpoint| %>

      <tr>
        <td><%= link_to endpoint[0], vehicle_trips_between_path(params[:vehicle_id], @from.name, endpoint[0]) %></td>
        <td><%= pluralize(endpoint[1], 'trip', 'trips') %></td>
      </tr>

<% end %>

    </tbody>
  </table>
</div></div>